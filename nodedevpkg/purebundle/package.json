{
  "name": "@innoai-tech/purebundle",
  "version": "0.2.4",
  "dependencies": {
    "@swc/core": "1.3.60",
    "@types/node": "^20.2.5",
    "vitest": "^0.31.1"
  },
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./index.mjs"
    },
    "./swc-plugin": "./target/wasm32-wasi/release/purebundle.wasm"
  },
  "files": [
    "index.d.ts",
    "index.mjs",
    "target/wasm32-wasi/release/purebundle.wasm"
  ],
  "license": "MIT",
  "scripts": {
    "initial": "rustup target add wasm32-wasi",
    "update": "cargo update",
    "lint": "cargo fmt --all -- --emit files",
    "build": "cargo build --target wasm32-wasi --release",
    "test": "pnpm run build && RUST_BACKTRACE=1 vitest --run --passWithNoTests --dir .",
    "prepublishOnly": "pnpm run build"
  },
  "type": "module"
}